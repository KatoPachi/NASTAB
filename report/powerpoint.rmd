---
title: "Project NaSTaB: Manipulation of Tax Refund by Monetary Giving"
author:
  - Hiroki Kato
date: "`r format(Sys.time(), '%Y/%m/%d')`"
output: 
  powerpoint_presentation:
    slide_level: 2
    reference_doc: template.pptx
---

```{r setup, include = FALSE, echo = FALSE}
knitr::opts_chunk$set(
  message = FALSE, 
  warning = FALSE, 
  echo = FALSE, 
  cache = FALSE,
  include = FALSE,
  fig.width = 15, fig.height = 10)
knitr::opts_knit$set(root.dir = "C:/Users/vge00/Desktop/NaSTaB")
```

```{r source_code_Summary}
knitr::read_chunk("script/#2Primitive.r")
```

```{r library}
```

```{r function}
```

```{r ggplot_report}
```

```{r data}
```

## 研究のモチベーション１

- 経済学における金銭的寄付のモチベーション：利他性 (Konow2010, JPubE)
  - 無条件の利他性： 純粋な利他性 (Becker, 1974 JPE), warm glow (Andreoni, 1989 JPE)
  - 条件つき利他性： 不平等回避 (Fehr and Schdmit, 1999 QJE), Distributional preference (Charness and Rabin, 2002 QJE) 

## 研究のモチベーション２

- 寄付に関する税制優遇措置があるとき，個人はrefundを得るために寄付をするのではないだろうか？（利己的なモチベーション）
  - bunching at refund-maximization point (Mortenson and Whitten, forthcoming AEJEP)
- 韓国の寄付税制が所得控除から税額控除に変更したときに生じるtax refundの損失を用いて，refundの調整をモチベーションに寄付していることを示したい．

## 税控除の割合

```{r dedcution_rate, include = TRUE}
```

## 所得控除から税額控除

```{r MTR, include = TRUE}
```

## 識別戦略

- Marginal tax rate in 2013 < 税額控除率
  - 寄付の価格が減少するので、寄付を増やす（利他的選好）
  - 1単位あたりのRefundが増えるので、寄付を減らすことで前回のRefund amountに調整（Refund motivation）
- Marginal tax rate in 2013 > 税額控除率
  - 寄付の価格が上昇するので、寄付を減らす（利他的選好）
  - 1単位あたりのRefundが増えるので、寄付を増やすことで前回のRefund amountに調整（Refund motivation）

## Difference in Refund Amount

```{r deductive_credit_amount, include = TRUE}
```

## Difference in Imputed Refund Amount

```{r deductive_credit_amount_imputed, include = TRUE}
```

## Primitive Regression

$$
Y_i 
= \alpha + \beta_1 Loss + \beta_2 Gain + \beta_3 Propensity 
+ \delta_1 Loss \cdot Propensity + \delta_2 Gain \cdot Propensity
+ \lambda X_i
$$

- $Y_i = \log{D_{14}}/\log{D_{13}}$
- $Loss$: Marginal tax rate in 2013 > 税額控除率
- $Gain$: Marginal tax rate in 2013 < 税額控除率
- $X$: 年齢（二乗項）、所得（2013年、2014年）、性別

## Propensity score 

- $Propensity$: 2013年に所得控除を受けていた確率をロジット分析（傾向スコア）
  - 説明変数：労働所得、年齢（二乗項）、学歴ダミー、性別ダミー、居住ダミー
  - 所得控除を受けていたかどうかを示す情報はあるが、欠損値が多すぎるために確率で代用する

## Distribution of Propensity Score

```{r propentisity_deduction, include = TRUE}
```

## Regression Result

```{r reg_giverate, include = TRUE}
```